Cluster nodes
=============

Each cluster starts with one root node.

Each root node can have one or more sub-nodes attached to it.

.. toctree::
    :maxdepth: 1

    root-nodes/index
    sub-nodes/index
